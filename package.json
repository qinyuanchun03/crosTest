{
  "name": "cf-cors-proxy",
  "version": "1.0.0",
  "description": "A deployable Cloudflare Worker that acts as a CORS proxy.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240403.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.47.0"
  }
}